# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
build/
dist/

# uv cache (keep uv.lock checked in — it pins versions)
.python-version

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code local overrides (keep settings.json, ignore .local.json)
.claude/settings.local.json

# Per-repo scratch scripts that agents use for ad-hoc verification
claude-tools/
