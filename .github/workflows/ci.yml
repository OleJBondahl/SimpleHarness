name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync

      - name: Lint
        run: uv run ruff check .

      - name: Format check
        run: uv run ruff format --check .

      - name: Type check
        run: uv run ty check

      - name: Architecture boundaries
        run: uv run lint-imports

      - name: Dead code
        run: uv run vulture src --min-confidence 80

      - name: Cognitive complexity (core modules)
        run: uv run complexipy src/simpleharness/core.py src/simpleharness/approver_core.py src/simpleharness/benchmark_core.py src/simpleharness/hooks --max-complexity-allowed 22

      - name: Tests
        run: uv run pytest
